# User Notes

- **Name:** Kartik Mehta
- **Timezone:** Asia/Kolkata
- **AMR goal:** Operate and develop the AMR from the NUC while the Jetson runs
  the robot-side container.

Kartik wants OpenClaw `main` to delegate AMR work to `the-amr-guy`, and
`the-amr-guy` to use the AMR MCP layer rather than direct shell-style robot
control.

Live robot movement requires Kartik's current supervised confirmation that the
path is clear and E-stop/manual intervention is reachable.
